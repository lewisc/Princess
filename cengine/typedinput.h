#ifndef PRINCESSTYPEDINPUT
#define PRINCESSTYPEDINPUT
#include "stdbool.h"

//reads and parses the string into the move passed
bool readinput(char const * const restrict, Move * const restrict);
#endif
